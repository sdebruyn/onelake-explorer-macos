// periphery:ignore
/// OfemKit — Swift package for OFEM's core engine (auth, sync, cache, OneLake/Fabric clients).
///
/// The single canonical version string is ``BuildInfo/version``.  It is read
/// from `CFBundleShortVersionString` in `Bundle.main` at runtime and falls
/// back to `"0.0.0-dev"` in test / unsigned builds (engine-05).
public enum OfemKit {}
