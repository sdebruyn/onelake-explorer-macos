<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>OneLake File Provider</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>OneLake Explorer for macOS — File Provider</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- LSMinimumSystemVersion is intentionally absent here: Xcode derives it
	     from MACOSX_DEPLOYMENT_TARGET (set via options.deploymentTarget.macOS
	     in project.yml) and embeds it automatically in the built bundle. A
	     duplicate hardcoded value here creates drift risk if the floor is ever
	     bumped. -->
	<!-- MSAL requires the redirect URI's custom scheme to be registered in
	     the running bundle's Info.plist. The FPE uses its own bundle ID
	     (dev.debruyn.ofem.fileprovider) for silent token acquisition, so it
	     needs its own msauth.<bundleID> URL scheme entry. Without this,
	     MSAL's local bundle-ID validation fails with -42011 before any
	     network call and every silent token acquisition returns an error,
	     leaving the Finder mount empty. Mirrors the entry in OneLake/Info.plist. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>dev.debruyn.ofem.fileprovider</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>msauth.dev.debruyn.ofem.fileprovider</string>
			</array>
		</dict>
	</array>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.fileprovider-nonui</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).FileProviderExtension</string>
		<key>NSExtensionFileProviderSupportsEnumeration</key>
		<true/>
		<key>NSExtensionFileProviderDocumentGroup</key>
		<string>$(TeamIdentifierPrefix)group.dev.debruyn.ofem</string>
	</dict>
</dict>
</plist>
