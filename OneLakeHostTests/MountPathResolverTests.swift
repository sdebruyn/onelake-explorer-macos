// MountPathResolverTests.swift
// Placeholder — no host-app mount-path helpers remain to test.
// "Open in Finder" resolves the URL via NSFileProviderManager.getUserVisibleURL,
// which is not unit-testable without a live File Provider domain.

import XCTest
